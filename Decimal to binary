#include <iostream>
using namespace std;

int solve(int n) {
  if(n == 0) { 
    return 0;
  }
  return n % 2 + 10 * solve(n/2); //formula to calculate binary number

}

int main() {
  cout << solve(10);
}
